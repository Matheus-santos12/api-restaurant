info:
  name: Show By Table Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/{{RESOURCE}}/table-session/4/total"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
