info:
  name: List Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/{{RESOURCE}}?name=sor"
  params:
    - name: name
      value: sor
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
