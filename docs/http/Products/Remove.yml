info:
  name: Remove
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/{{RESOURCE}}/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
