info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/{{RESOURCE}}/16"
  body:
    type: json
    data: |-
      {
        "name": "Sorvete de graviola",
        "price": 15
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
