info:
  name: Closed
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/{{RESOURCE}}/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
